import React from 'react';
/* import { useState } from 'react'; */
/* const axios = require('axios'); */
function PostPage({ MovePost }) {
	/* 	const [postId, setPostId] = useState(''); */

	/* 	axios
		.get(`https://jsonplaceholder.typicode.com/posts/${MovePost.id}`)
		.then(res => {
			res && res.length > 0 && res.map(elem => setPostId(elem.id));
			console.log(MovePost.map(item => item.id));
		})
		.catch(console.error());
 */

	return (
		<div>
			<h1>post</h1>
		</div>
	);
}

export default PostPage;
